import { Outlet } from "react-router-dom";
import React from "react";

const AuthLayoutSiswa = () => {
  return (
    <div id="auth-layout-siswa">
      <Outlet />
    </div>
  );
};

export default AuthLayoutSiswa;
