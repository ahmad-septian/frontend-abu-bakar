import { Outlet } from "react-router-dom";
import React from "react";

const AuthLayoutGuru = () => {
  return (
    <div id="auth-layout-guru" >
      <Outlet />
    </div>
  );
};

export default AuthLayoutGuru;
